package ru.otus.spring.service.util

object Const {
    val MANY_WHITESPACES_REGEX = Regex("\\s+")
    val QUESTION_REGEX = Regex("\\s*\\?\\s*")
    val COMMA_REGEX = Regex("\\s*,\\s*")
    const val NEW_LINE = "\n"
}
